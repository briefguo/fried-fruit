import React from 'react'
import styled from 'styled-components'

const LogoAsset = props => (
  <img {...props} src="https://oss.briefguo.com/assets/logo.png" alt="" />
)

export const Logo = styled(LogoAsset)``
